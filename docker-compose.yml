services:
  cli:
    build:
      context: .
      target: runtime
    volumes:
      - ./output:/app/output

  dev:
    build:
      context: .
      target: development
    volumes:
      - .:/app
    working_dir: /app
    stdin_open: true
    tty: true
    command: sh